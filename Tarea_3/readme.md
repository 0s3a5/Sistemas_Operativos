Wena los cabros 
se procede a hacer speedrun de tarea 3

Para la implementacion de la tarea se debn descargar lo de simple
solo 1 .cpp y un solo .h

implemnetacion de la tarea
decargar los dos archivos o crearlos en la misma carpeta

y ejecutar

g++ -o tarea tarea.cpp
./tarea

Para la implementacion en partes se descarga lo de partes

la tarea esta divida en partes(por algo el archivo se llama asi), aunque el entregable es solo un cpp y un solo .h 
para hacerlo mas sencillo de explicar prefiero hacerlo en archivos diferentes 

la tarea cuenta con 2 .h y 3 .cpp
los .h son las definiciones de las clases, ¿se podia hacer en uno solo? si
¿era util? no, sale mas facil pedir las clases por separado para hacerlo mas eficiente 
de igual modo la version de un solo .h funciona eficiente solo que mas compacto 

si se llega a descargar las partes por separado es necesario tener todos los archivos en una sola carpeta
y aparte se debe escribir en la terminal

g++ -o tarea main.cpp memoria.cpp proceso.cpp
./tarea
